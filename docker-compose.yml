services:
  app:
    build: .
    container_name: stock_pulse
    ports:
      - "8080:8080"
    environment:
      - TZ=Asia/Seoul
    networks:
      - app_default

networks:
  app_default:
    external: true